.container			//// Основной контейнер с тенями по бокам, шириной 1200px, основной фон: #fff;

.wrapper 			// Контейнер, шириной 1200px;
	--pink 			// Модификатор для .wrapper, основной фон: #f9f3ef;
	--blue 			// Модификатор для .wrapper, основной фон: #e8f4fa;
	--white			// Модификатор для .wrapper, основной фон: #fff;
	--gray			// Модификатор для .wrapper, основной фон - градиент: #fbf9f7, #fbfaf9;

.header 			// Шапка сайта

.group 				// Пустой класс, без стилей. Предназначен для обозначения группировки необходимых блоков.
					// Модификатор: --parent-class
					// Модификатор: --*перечисление блоков*
					




git add .
git status // посмотреть какие изменения произошли
git commit -m 'Some descriptive commit message'
git push origin master
git checkout gh-pages // сменить ветку на gh-pages
git rebase master // синхронизировать gh-pages c master (git merge master)
git push origin gh-pages // отправить ветку на сервер.
git checkout master // вернуться в master


<<<<<<< HEAD

added line
=======
added line
>>>>>>> ext
